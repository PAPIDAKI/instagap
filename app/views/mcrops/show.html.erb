<p id="notice"><%= notice %></p>

<p>
  <strong>Code fyta:</strong>
  <%= @mcrop.code_fyta %>
</p>

<p>
  <strong>Kathh:</strong>
  <%= @mcrop.kathg %>
</p>

<p>
  <strong>Eidos:</strong>
  <%= @mcrop.eidos %>
</p>

<p>
  <strong>Gonoma:</strong>
  <%= @mcrop.gonoma %>
</p>

<p>
  <strong>Genenral:</strong>
  <%= @mcrop.general %>
</p>

<p>
  <strong>Code:</strong>
  <%= @mcrop.code %>
</p>

<p>
  <strong>Klartex:</strong>
  <%= @mcrop.klartext %>
</p>


<%= link_to 'Edit', edit_mcrop_path(@mcrop) %> |
<%= link_to 'Back', mcrops_path %>

<hr>

<h3>Approved Chem List (<%=@mfcs.count%> approved solutions)</h3>
<p><%=link_to "Printable List(PDF)" ,mcrop_path(@mcrop,format:"pdf")%> </p>
  <table class="table">
    <thead>
    <tr>
      <th>Trade name</th>
      <th>Category</th>
      <th>Prime Igedient</th>
      <th>Reasoning</th>
      <th>Dosage</th>

      <th>Application Unit</th>
      <th>Application Method</th>

      <th>Phi</th>

      <th>Approval Date</th>
      <th>Expiration Date</th>
      <th colspan="3"></th>
    </tr>
    </thead>

    <tbody>
    <% @mfcs.each do |mfc| %>
        <tr>
          <td><%= mfc.try(:gbonoma).upcase %></td>
          <td><%=mfc.mkatigori.kathgoria%></td>
          <td><%= mfc.mfarmako.systatiks.first.try(:onomadron).upcase %></td>
          <td><%=mfc.mfarmako.mfarmkasts.where(:mcrop_id==params[:code_fyta]).first.try(:mastfarms).try(:last).try(:masthenium).try(:astenia)%></td>
          <td><%=mfc.mfarmako.mfarmkasts.where(:mcrop_id==params[:code_fyta]).first.try(:mastfarms).try(:last).try(:dosh_farm)%></td>
          <td><%=mfc.mfarmako.mfarmkasts.where(:mcropi_id==params[:code_fyta]).first.try(:mastfarms).try(:last).try(:mdosh).try(:dosh)%></td>
          <td><%=truncate(mfc.mfarmako.try(:tropo_efar))%></td>
          

          <td><%= mfc.mfarmako.mepembashes.where(:mcrop_id==params[:code_fyta]).first.try(:diast_pros) %></td>
          
          <td><%=mfc.mfarmako.try(:hmer_egris)%>
          <td><%= mfc.mfarmako.try(:telos_egri) %></td>

          
        </tr>
    <%end%>


    </tbody>
  </table>



